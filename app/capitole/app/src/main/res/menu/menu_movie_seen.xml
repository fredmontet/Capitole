<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".presentation.activity.MovieToSeeDetailActivity">

    <item
        android:id="@+id/action_edit_rating"
        android:icon="@drawable/ic_description_white_48dp"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:title="Mark as seen"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/action_delete"
        android:icon="@drawable/ic_delete_white_48dp"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:title="Remove"
        app:showAsAction="ifRoom|collapseActionView" />

</menu>
